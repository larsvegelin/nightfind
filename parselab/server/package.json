{
  "name": "parselab-server",
  "version": "1.0.0",
  "private": true,
  "description": "ParseLab: dashboard + Website uitlezen vanaf de server (met proxies)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  }
}
